import { rsp } from "./index"

it('true is true', () => {
  expect(true).toEqual(false);
});

// it('p1 rock vs p2 paper is p2wins', () => {
//   expect(rsp('rock', 'paper')).toEqual('p2wins');
// });